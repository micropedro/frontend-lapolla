// sum.test.js
/* import { expect, test } from 'vitest'
import { getTicketCode } from '../services/utils' */
/* import { string } from 'joi' */

/* test('retorna 5 digitos ramdon', () => {
    expect(getTicketCode().length).toBe(5)
}) */

/* test('retorna un tipo de dato string', () => {
    expect(typeof getTicketCode()).toBe('string')
})

test('genera 100 strings y todos son diferentes', () => {
    let strings = []
    for (let i = 0; i < 10000; i++) {
        strings.push(getTicketCode())
    }
    const tieneValoresIguales = () => new Set(strings).size === strings.length
    expect(tieneValoresIguales()).toBe(true)
})

test('retorna siempre strings diferentes', () => {
    const string1 = getTicketCode()
    const string2 = getTicketCode()
    expect(string1 !== string2).toBe(true)
}) */